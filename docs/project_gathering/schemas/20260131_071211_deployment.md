Deployment Diagram

+---------------------------+
| Developer Machine          |
| - kmi CLI                  |
| - FastAPI proxy (uvicorn)  |
| - local state/logs/trace   |
+------------+--------------+
             |
             | HTTPS
             v
+---------------------------+
| Kimi API (Upstream)        |
+---------------------------+

Remote proxy binding is allowed only when explicitly enabled and secured:
- KMI_PROXY_ALLOW_REMOTE=1
- KMI_PROXY_TOKEN set
- TLS termination required (or KMI_PROXY_REQUIRE_TLS=0 override)
